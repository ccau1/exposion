import React from "react";
import { ElementFormProps } from "../../typings";
import { GroupProperties } from ".";

export default ({}: ElementFormProps<GroupProperties>) => {
  return <div>group form</div>;
};
