import React from "react";
import { ElementFormProps } from "../../typings";
import { RoomProperties } from ".";

export default ({}: ElementFormProps<RoomProperties>) => {
  return <div>room form</div>;
};
